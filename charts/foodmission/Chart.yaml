apiVersion: v2
name: foodmission
version: 0.1.0
description: FOODMISSION — API, Keycloak, and PostgreSQL (CNPG)
appVersion: "1.0.0"

# Note: sealed-secrets is installed cluster-wide, not as a subchart dependency
# Install once: helm install sealed-secrets sealed-secrets/sealed-secrets -n kube-system

dependencies:
  - name: valkey
    version: "6.2.13"
    repository: https://charts.bitnami.com/bitnami
    condition: valkey.enabled
