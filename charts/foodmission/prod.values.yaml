# Production environment

global:
  namespace: foodmission-prod
  tlsSecret: foodmission-tls-cert-prod
  cnpg:
    clusterName: foodmission-pg-prod

cnpg:
  instances: 5
  storageSize: 50Gi

  backup:
    enabled: true
    barmanStoreName: foodmission-backup-store
    destinationPath: s3://foodmission-production/backups
    endpointURL: https://hel1.your-objectstorage.com

  scheduledBackup:
    enabled: true

  # # Bootstrap from existing backup
  # bootstrap:
  #   recovery:
  #     source: foodmission-pg-prod

  # # Define the external cluster (the backup source)
  # externalClusters:
  #   - name: foodmission-pg-prod
  #     barmanObjectStore:
  #       destinationPath: s3://foodmission-production/backups
  #       endpointURL: https://hel1.your-objectstorage.com
  #       serverName: foodmission-pg # new one is foodmission-pg-prod
  #       s3Credentials:
  #         accessKeyId:
  #           name: foodmission-secrets
  #           key: ACCESS_KEY_ID
  #         secretAccessKey:
  #           name: foodmission-secrets
  #           key: ACCESS_SECRET_KEY
  #       wal:
  #         compression: gzip

api:
  image: ghcr.io/reedu-reengineering-education/foodmission-data-framework:0.2.2
  replicaCount: 3
  extraEnv:
    - name: MONGODB_OFF_URL
      value: "mongodb://admin:TestPassword123@mongodb-service.off-test.svc.cluster.local:27017/?authSource=admin"
  translations:
    enabled: true
  opentelemetry:
    enabled: "true"

keycloak:
  image: ghcr.io/reedu-reengineering-education/foodmission-keycloak:0.1.2
  replicaCount: 2

valkey:
  enabled: true
